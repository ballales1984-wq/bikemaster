{
  "name": "bikemaster",
  "version": "1.0.0",
  "description": "BikeMaster - Cycling Performance Intelligence (backend Python/FastAPI)",
  "private": true,
  "scripts": {
    "info": "echo \"Frontend served by FastAPI from bike_analyzer/backend/static/\"",
    "prepare": "git rev-parse --git-dir >/dev/null 2>&1 && [ \"$CI\" != \"true\" ] && lefthook install || true"
  },
  "keywords": ["bike", "cycling", "performance", "fastapi"],
  "license": "MIT",
  "devDependencies": {
    "lefthook": "^1.7.0"
  }
}
