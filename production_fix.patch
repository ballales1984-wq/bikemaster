From fee00c6 Mon Sep 17 00:00:00 2001
From: BikeMaster <bikemaster@local>
Date: Fri, 3 Jul 2026 10:00:00 +0000
Subject: [PATCH] production: security fixes and Dockerfile path correction

diff --git a/bike_analyzer/backend/api/routes.py b/bike_analyzer/backend/api/routes.py
--- a/bike_analyzer/backend/api/routes.py
+++ b/bike_analyzer/backend/api/routes.py
@@ -1577,7 +1577,7 @@
     )
 
 
-@router.get("/knowledge")
-async def list_knowledge():
+@router.get("/knowledge")
+async def list_knowledge(current_user: dict = Depends(get_current_user)):
     from ..analytics.knowledge_base import get_kb_stats
 
@@ -1591,7 +1591,8 @@
         "count": len(results),
         "query": query,
         "topics_matched": sorted({r["topic"] for r in results}),
     }
 
-@router.get("/traffic/road-types")
-async def get_road_types(
+@router.get("/traffic/road-types")
+async def get_road_types(
     lat: float = Query(...), lon: float = Query(...), radius_km: float = Query(2.0)
 ):
     if lat and lon:
